# Project version
version=1.0.0

# Gradle settings
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# GitHub Package Registry credentials (leave empty, set via env or ~/.gradle/gradle.properties)

